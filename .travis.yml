os: linux

before_install:
    - wget https://github.com/gohugoio/hugo/releases/download/v0.54.0/hugo_0.54.0_Linux-64bit.deb
    - sudo dpkg -i hugo*.deb
    - hugo version

script:
    - make build

after_success:
    - cat build/dev/index.html
